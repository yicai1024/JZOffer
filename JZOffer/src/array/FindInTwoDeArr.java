package array;

public class FindInTwoDeArr {
	public boolean Find(int target, int [][] array) {
        boolean f=false;
        if(array.length>0){
        	int row=array.length;//ÐÐ
        	int col=array[0].length;//ÁÐÊý
        	
        
        	
        }
        
        return f;
    }
}
